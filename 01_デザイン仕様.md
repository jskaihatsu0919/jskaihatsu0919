# 01 デザイン仕様

作成済みの3帳票（個人鑑定・相性診断・組織診断）は、すべてこの仕様で統一されています。
新しい帳票を作るときは `lib/deck_kit.js` を使い、この仕様から外れないようにしてください。

---

## 配色

| 役割 | 色 | 用途 |
|---|---|---|
| 主色 | `1C2B3A` 墨紺 | 表紙・章扉・締めの背景、見出し文字、中心セル。全体の6〜7割 |
| 地色 | `F4F1E8` 生成り | 本文スライドの背景 |
| 差し色 | `B79A5B` 金 | ラベル、章番号、円相、締めの一文 |
| 注意 | `9C3B2E` 朱 | 反発・リスク・要配慮 |
| 本文 | `24303D` 墨 | 本文テキスト |
| 補足 | `6E7681` 灰 | キャプション |
| 罫 | `D8D2C2` | カード枠・区切り線 |

**五行カラー**（数値の色分けに必ずこれを使う）

| 木 | 火 | 土 | 金 | 水 |
|---|---|---|---|---|
| `4A7C59` | `9C3B2E` | `B08D3F` | `7C8794` | `2F4B6E` |

---

## タイポグラフィ

- **見出し・干支・数値** … 游明朝
- **本文・ラベル** … 游ゴシック

游書体は Windows・Mac の PowerPoint 双方に標準搭載されているため、環境差が出ません。
明朝を見出しに絞り、本文をゴシックにすることで格調と可読性を両立させています。

| 要素 | サイズ |
|---|---|
| 表紙タイトル | 44pt 太字 |
| スライド見出し | 26pt 太字 |
| 章扉タイトル | 38pt 太字 |
| セクション見出し | 16pt 太字 |
| カード見出し | 13.5pt 太字 |
| 本文 | 11〜12.5pt（行間は文字サイズ×1.75） |
| キャプション | 9.5〜10pt |

---

## 装飾モチーフ

**円相のみ**を使います。禅画の円相を、金の細線・透明度84%で各スライドに1〜2個だけ配置します。
明スライドは右下（11.55, 5.85, 直径2.6）、濃紺スライドは右上に大小2つ。スライド外へはみ出させて構いません。

**使ってはいけないもの**

- 見出し下のアクセント線
- 帯・サイドストライプ・カードの片側だけの色枠
- グラデーション（pptxgenjs 非対応）
- 本文の中央揃え（見出しのみ中央可）

---

## レイアウトの決まり

- キャンバスは `LAYOUT_WIDE`（13.333 × 7.5 インチ）
- 左右の余白は 0.85 インチ、コンテンツ幅は 11.6 インチに固定
- 見出しは y=0.46、リード文は y=1.12（高さ0.72＝2行ぶん）
- 本文ブロックの開始は y=1.85〜1.9
- 下端は y=7.0 まで。それ以下には置かない
- ブロック間は 0.2〜0.25 インチ

**濃紺と生成りのサンドイッチ構造** … 表紙と締めは濃紺、章扉も濃紺、間の本文は生成り。
章が3つ以上ある帳票では、章扉を挟んでリズムを作ります。

---

## 部品（deck_kit.js の C.*）

| 部品 | 用途 |
|---|---|
| `C.cover` | 表紙 |
| `C.closing` | 締め（濃紺＋一文＋箇条書き） |
| `C.toc` | 目次 |
| `C.cardsRow` | 横並びカード2〜4枚。`head:true` で見出し帯つき、`dark:true` で濃紺 |
| `C.numberedGrid` | 丸数字つきグリッド（3×2 など） |
| `C.twoPanel` | 二欄比較（活きる／消耗、Do／Don't） |
| `C.table` | 表。列幅の合計は 11.6 |
| `C.barRows` | 横棒の比較 |
| `C.humanStar` | 人体星図（3×3・中心セルのみ濃紺） |
| `C.gogyoCross` | 五行エネルギーの十字図。円の直径が数値に比例 |
| `C.timeline` | 大運の年表10コマ。`now` で現在地、`shade` で天中殺 |
| `C.meishikiCard` | 命式サマリー（相性・組織診断で複数人を並べるとき） |
| `C.bigStat` | 大きな数字の訴求 |
| `C.stepRows` | 付録の番号つき解説行 |

---

## 品質チェック（必須）

```bash
node build.js
python3 /mnt/skills/public/pptx/scripts/office/validate.py 出力.pptx
python3 qa.py 出力.pptx          # 境界とテキスト溢れの機械検査
```

`qa.py` の「はみ出し」は円相の意図的なブリードなので無視します。
「溢れ懸念」が出たら、文字数を減らすか箱を広げてください。

画像プレビューが使えるときは、`soffice.py --convert-to pdf` → `pdftoppm` で目視も行ってください。

---

## pptxgenjs の注意点

- 色は `#` なし・6桁のみ。`"#FF0000"` や8桁はファイルを壊す
- オプションオブジェクトを複数の `add*` で使い回さない（内部で書き換えられる）
- `rectRadius` は `ROUNDED_RECTANGLE` のみ有効
- 箇条書きは `bullet: true`。文中に `・` や `•` を書かない
- 影の `offset` は 0 以上
- テキストボックスには内部余白があるため、図形と揃えるときは `margin: 0`
